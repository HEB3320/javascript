const person ={

    name :"Engin Bekem",
    age  :54
};

const greeting = `My name is ${person.name} and I am ${person.age} years old.` ;
console.log(greeting);

// function kisa yazilisi
const cap = str=>str.toUpperCase();
 
const str = "hello";

console.log(cap(str));

